FROM n8nio/n8n

# Optional: Set default timezone
ENV GENERIC_TIMEZONE=Europe/Madrid

# Optional: Enable basic auth for security
ENV N8N_BASIC_AUTH_ACTIVE=true
ENV N8N_BASIC_AUTH_USER=admin
ENV N8N_BASIC_AUTH_PASSWORD=strong-password

# Optional: Define a base URL (useful if using a subpath or proxy)
# ENV N8N_EDITOR_BASE_URL=https://your-n8n.onrender.com

# Expose port (Render auto-detects this)
EXPOSE 5678
